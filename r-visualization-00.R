# clear environment
rm(list = ls())

# load tidyver, a comprehensive data analytic package
install.packages('tidyverse')

# install data sets (we will only load data as neccessary
install.packages(c('babynames','fueleconomy','nasaweather','nycflights13'))




